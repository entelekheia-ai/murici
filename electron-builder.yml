appId: com.entelekheia.murici
productName: Murici
copyright: "Copyright © 2026 Danilo Borges. All rights reserved."

fileAssociations:
  - ext: agent
    name: Agent File
    role: Editor
    icon: dot-agent-icon

directories:
  output: dist-electron
  buildResources: electron/assets

# Only the compiled electron process + resources
files:
  - electron-dist/**
  - "!node_modules/**"
  - node_modules/@dot-agent/**
  - "!node_modules/@dot-agent/**/target/**"
  - "!node_modules/@dot-agent/**/src/**"
  - "!node_modules/@dot-agent/**/tests/**"
  - "!node_modules/@dot-agent/**/node_modules/**"
  - "!node_modules/@dot-agent/**/*.rs"
  - "!node_modules/@dot-agent/**/Cargo.*"
  - "!node_modules/@dot-agent/**/build.rs"
  - node_modules/jszip/**
  - node_modules/electron-updater/**
  - node_modules/builder-util-runtime/**

# Next.js standalone server packed into resources/server
extraResources:
  - from: .next/standalone
    to: server
    filter:
      - "**/*"

# WASM and kernel binaries must be outside ASAR
asarUnpack:
  - "**/*.wasm"
  - "**/dot-agent-kernel/**"

mac:
  target:
    - target: dmg
      arch: [x64, arm64]
  category: public.app-category.productivity
  darkModeSupport: true

win:
  target:
    - target: nsis
      arch: [x64]

linux:
  target:
    - target: AppImage
      arch: [x64]

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

publish:
  provider: github
  releaseType: release
